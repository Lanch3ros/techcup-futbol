services:
  postgres:
    image: postgres:16
    container_name: techcup-postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-techcup}
      POSTGRES_USER: ${DB_USERNAME:-techcup}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-techcup}
    ports:
      - "5433:5432"
    volumes:
      - techcup_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  techcup_data:
